{{#if optionsPage}}
	<script src="index.js" charset="utf-8" defer></script>
    {{>optionsPage}}
{{/if}}

{{#unless optionsPage}}
    <script src="gamePal.js" charset="utf-8" defer></script>
    {{#if gameDice}}
        {{>dice}}
    {{/if}}
    
    {{#each users}}
        {{>playerCard}}
    {{/each}}
{{/unless}}